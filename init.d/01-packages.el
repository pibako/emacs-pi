(require 'cask "~/.cask/cask.el")
(cask-initialize)
(require 'pallet)                       ; all new packages will
                                        ; automatically be added or
                                        ; removed to cask file
